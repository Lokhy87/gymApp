{% extends 'base.html.twig' %}

{% block title %}Exercises{% endblock %}

{% block body %}
    <h1>Exercises</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercise.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exercise.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercises_index') }}">back to list</a>

    <a href="{{ path('app_exercises_edit', {'id': exercise.id}) }}">edit</a>

    {{ include('exercises/_delete_form.html.twig') }}
{% endblock %}
