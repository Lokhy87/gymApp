{% extends 'base.html.twig' %}

{% block title %}WorkSplit{% endblock %}

{% block body %}
    <h1>WorkSplit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_split.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work_split.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work_split.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_split_index') }}">back to list</a>

    <a href="{{ path('app_work_split_edit', {'id': work_split.id}) }}">edit</a>

    {{ include('work_split/_delete_form.html.twig') }}
{% endblock %}
