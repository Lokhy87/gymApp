<form method="post" action="{{ path('app_training_method_delete', {'id': training_method.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_method.id) }}">
    <button class="btn">Delete</button>
</form>
