{% extends 'base.html.twig' %}

{% block title %}TrainingMethod{% endblock %}

{% block body %}
    <h1>TrainingMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_method.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_method.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training_method.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_method_index') }}">back to list</a>

    <a href="{{ path('app_training_method_edit', {'id': training_method.id}) }}">edit</a>

    {{ include('training_method/_delete_form.html.twig') }}
{% endblock %}
