{% extends 'base.html.twig' %}

{% block title %}TrainingLevel index{% endblock %}

{% block body %}
    <h1>TrainingLevel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for training_level in training_levels %}
            <tr>
                <td>{{ training_level.id }}</td>
                <td>{{ training_level.name }}</td>
                <td>
                    <a href="{{ path('app_training_level_show', {'id': training_level.id}) }}">show</a>
                    <a href="{{ path('app_training_level_edit', {'id': training_level.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_training_level_new') }}">Create new</a>
{% endblock %}
