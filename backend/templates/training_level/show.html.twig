{% extends 'base.html.twig' %}

{% block title %}TrainingLevel{% endblock %}

{% block body %}
    <h1>TrainingLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_level_index') }}">back to list</a>

    <a href="{{ path('app_training_level_edit', {'id': training_level.id}) }}">edit</a>

    {{ include('training_level/_delete_form.html.twig') }}
{% endblock %}
