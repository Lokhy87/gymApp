<form method="post" action="{{ path('app_training_level_delete', {'id': training_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_level.id) }}">
    <button class="btn">Delete</button>
</form>
