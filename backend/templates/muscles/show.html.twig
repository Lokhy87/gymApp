{% extends 'base.html.twig' %}

{% block title %}Muscles{% endblock %}

{% block body %}
    <h1>Muscles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ muscle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ muscle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_muscles_index') }}" class="btn">back to list</a>

    <a href="{{ path('app_muscles_edit', {'id': muscle.id}) }}" class="btn">edit</a>

    {{ include('muscles/_delete_form.html.twig') }}
{% endblock %}
