{% extends 'base.html.twig' %}

{% block title %}ExercisesMuscles index{% endblock %}

{% block body %}
    <h1>ExercisesMuscles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Role</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for exercises_muscle in exercises_muscles %}
            <tr>
                <td>{{ exercises_muscle.id }}</td>
                <td>{{ exercises_muscle.role }}</td>
                <td>
                    <a href="{{ path('app_exercises_muscles_show', {'id': exercises_muscle.id}) }}">show</a>
                    <a href="{{ path('app_exercises_muscles_edit', {'id': exercises_muscle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_exercises_muscles_new') }}">Create new</a>
{% endblock %}
