<form method="post" action="{{ path('app_exercises_muscles_delete', {'id': exercises_muscle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercises_muscle.id) }}">
    <button class="btn">Delete</button>
</form>
