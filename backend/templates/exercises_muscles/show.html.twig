{% extends 'base.html.twig' %}

{% block title %}ExercisesMuscles{% endblock %}

{% block body %}
    <h1>ExercisesMuscles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercises_muscle.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ exercises_muscle.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercises_muscles_index') }}">back to list</a>

    <a href="{{ path('app_exercises_muscles_edit', {'id': exercises_muscle.id}) }}">edit</a>

    {{ include('exercises_muscles/_delete_form.html.twig') }}
{% endblock %}
