{% extends 'base.html.twig' %}

{% block title %}TrainingGoal{% endblock %}

{% block body %}
    <h1>TrainingGoal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_goal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_goal.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training_goal.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_goal_index') }}">back to list</a>

    <a href="{{ path('app_training_goal_edit', {'id': training_goal.id}) }}">edit</a>

    {{ include('training_goal/_delete_form.html.twig') }}
{% endblock %}
